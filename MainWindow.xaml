<!-- Copyright (c) 2025 Qz3rK -->
<!-- License: MIT (https://opensource.org/licenses/MIT) -->

<Window x:Class="DefragManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DefragManager"
        mc:Ignorable="d"
        Title="DeFRaG Manager" 
        Height="590" 
        Width="800"
        MinWidth="800"
        MaxWidth="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    
    <Window.Resources>
        <local:FavoriteToColorConverter x:Key="FavoriteColorConverter"/>
        <local:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <FontFamily x:Key="EternalUiFont">pack://application:,,,/Assets/Fonts/#Eternal UI</FontFamily>

        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}"/>
        <Style TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type Label}}"/>
        <Style TargetType="{x:Type Control}" BasedOn="{StaticResource {x:Type Control}}"/>

        <Style TargetType="{x:Type DataGridRow}">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        
        <!-- Цвета для темной темы -->
        <SolidColorBrush x:Key="BackgroundColor" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="TextColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="ButtonBackground" Color="#3D3D3D"/>
        <SolidColorBrush x:Key="AlternatingRowColor" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="ThumbnailBackground" Color="#3A3A3A"/>
        <SolidColorBrush x:Key="TextBoxBackground" Color="#252525"/>
        <SolidColorBrush x:Key="SelectedTabColor" Color="#3D3D3D"/>
        <SolidColorBrush x:Key="ScrollBarThumb" Color="#606060"/>
        <SolidColorBrush x:Key="ScrollBarBackground" Color="#252525"/>
        <SolidColorBrush x:Key="ScrollBarArrow" Color="#808080"/>
        
        <Style x:Key="TimeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="RowBackground" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource AlternatingRowColor}"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="RowHeight" Value="50"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <!-- ... остальные свойства ... -->
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <DataTemplate x:Key="ThumbnailColumnTemplate">
            <Grid Width="100" Height="50" Margin="0">
                <Rectangle Fill="{DynamicResource ThumbnailBackground}" Stretch="Fill" Margin="0"/>
                <Image Source="{Binding Thumbnail}" 
                       Stretch="UniformToFill"
                       Margin="0"
                       RenderOptions.BitmapScalingMode="LowQuality">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Thumbnail}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
                <TextBlock Text="No preview" 
                           Foreground="{DynamicResource TextColor}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Margin="0"
                           Visibility="{Binding Thumbnail, Converter={StaticResource NullToVisibilityConverter}}"/>
            </Grid>
        </DataTemplate>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource TextBoxBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextColor}"/>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border Name="Border" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,1,1,0"
                                    Margin="0,0,-4,0">
                                <ContentPresenter x:Name="ContentSite"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Center"
                                                ContentSource="Header"
                                                Margin="12,2,12,2"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource SelectedTabColor}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                            
                            <!-- Вертикальная полоса прокрутки - всегда видимая -->
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                     Grid.Column="1"
                                     Grid.Row="0"
                                     Orientation="Vertical"
                                     Cursor="Arrow"
                                     ViewportSize="{TemplateBinding ViewportHeight}"
                                     Maximum="{TemplateBinding ScrollableHeight}"
                                     Minimum="0"
                                     Value="{TemplateBinding VerticalOffset}"
                                     Visibility="Visible"> <!-- Изменено на всегда Visible -->
                                <ScrollBar.Style>
                                    <Style TargetType="ScrollBar">
                                        <Setter Property="Width" Value="8"/>
                                        <Setter Property="Background" Value="{DynamicResource ScrollBarBackground}"/>
                                        <Setter Property="Opacity" Value="0.5"/> <!-- Полупрозрачная по умолчанию -->
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ScrollBar">
                                                    <Grid>
                                                        <Track x:Name="PART_Track" IsDirectionReversed="true">
                                                            <Track.Thumb>
                                                                <Thumb Background="{DynamicResource ScrollBarThumb}">
                                                                    <Thumb.Template>
                                                                        <ControlTemplate TargetType="Thumb">
                                                                            <Border Background="{TemplateBinding Background}"
                                                                                    CornerRadius="4"/>
                                                                        </ControlTemplate>
                                                                    </Thumb.Template>
                                                                </Thumb>
                                                            </Track.Thumb>
                                                        </Track>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <!-- При наведении или активности делаем полностью видимой -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Opacity" Value="1"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="True">
                                                <Setter Property="Opacity" Value="1"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ScrollBar.Style>
                            </ScrollBar>
                            
                            <!-- Горизонтальная полоса (оставлена как было) -->
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                     Grid.Column="0"
                                     Grid.Row="1"
                                     Orientation="Horizontal"
                                     Cursor="Arrow"
                                     ViewportSize="{TemplateBinding ViewportWidth}"
                                     Maximum="{TemplateBinding ScrollableWidth}"
                                     Minimum="0"
                                     Value="{TemplateBinding HorizontalOffset}"
                                     Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Стиль для кнопок заголовка окна -->
        <Style x:Key="TitleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A3A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

<Grid>
    <!-- Основной контейнер с закруглёнными углами -->
    <Border Background="{DynamicResource BackgroundColor}" 
            CornerRadius="2" 
            SnapsToDevicePixels="True">
        
        <!-- Главный контейнер с явным разделением на области -->
        <Grid>
            <!-- Определяем области для изменения размера -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3"/> <!-- Левый край -->
                <ColumnDefinition Width="*"/>  <!-- Центр -->
                <ColumnDefinition Width="3"/> <!-- Правый край -->
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="3"/>   <!-- Верхний край -->
                <RowDefinition Height="*"/>    <!-- Центр -->
                <RowDefinition Height="3"/>   <!-- Нижний край -->
            </Grid.RowDefinitions>
            
            <!-- Верхний край (полоса) -->
            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" 
                    Cursor="SizeNS" Background="Transparent"
                    MouseLeftButtonDown="ResizeWindow_Top"/>
            
            <!-- Нижний край (полоса) -->
            <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
                    Cursor="SizeNS" Background="Transparent"
                    MouseLeftButtonDown="ResizeWindow_Bottom"/>
            
            <!-- Углы (перекрывают пересечения полос) -->
            <!-- Левый верхний -->
            <Border Grid.Row="0" Grid.Column="0" 
                    Cursor="SizeNWSE" Background="Transparent"
                    MouseLeftButtonDown="ResizeWindow_TopLeft"/>
            
            <!-- Правый верхний -->
            <Border Grid.Row="0" Grid.Column="2" 
                    Cursor="SizeNESW" Background="Transparent"
                    MouseLeftButtonDown="ResizeWindow_TopRight"/>
            
            <!-- Левый нижний -->
            <Border Grid.Row="2" Grid.Column="0" 
                    Cursor="SizeNESW" Background="Transparent"
                    MouseLeftButtonDown="ResizeWindow_BottomLeft"/>
            
            <!-- Правый нижний -->
            <Border Grid.Row="2" Grid.Column="2" 
                    Cursor="SizeNWSE" Background="Transparent"
                    MouseLeftButtonDown="ResizeWindow_BottomRight"/>
            
            <!-- Основное содержимое окна -->
            <Grid Grid.Row="1" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="27"/> <!-- Заголовок -->
                    <RowDefinition Height="*"/>  <!-- Контент -->
                </Grid.RowDefinitions>
                
                <!-- Панель заголовка -->
                <Border Grid.Row="0" 
                        Background="#1E1E1E" 
                        CornerRadius="5,5,0,0"
                        MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <TextBlock Text="{Binding WindowTitle}" 
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"
                                   FontFamily="{StaticResource EternalUiFont}"/>
                        
                        <!-- Кнопки управления окном -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="─" Style="{StaticResource TitleButtonStyle}" Click="MinimizeWindow"/>
                            <Button Content="□" Style="{StaticResource TitleButtonStyle}" Click="MaximizeWindow"/>
                            <Button Content="✕" Style="{StaticResource TitleButtonStyle}" Click="CloseWindow"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Основное содержимое -->
                <Border Grid.Row="1" CornerRadius="0,0,5,5">
                    <TabControl Background="{DynamicResource BackgroundColor}" SelectionChanged="TabControl_SelectionChanged">
                        <TabItem x:Name="AllMapsTab" Header="All Maps">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <TextBox x:Name="SearchBox" 
                                         Margin="5" 
                                         TextChanged="SearchBox_TextChanged"
                                         Grid.Row="0"/>
                                
                                <ScrollViewer x:Name="MapsGridScroll" 
                                              VerticalScrollBarVisibility="Auto"
                                              ScrollChanged="MapsGrid_ScrollChanged"
                                              Grid.Row="1"
                                              PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                    <DataGrid x:Name="MapsGrid" 
                                              AutoGenerateColumns="False" 
                                              HeadersVisibility="None"
                                              VirtualizingPanel.ScrollUnit="Pixel"
                                              RowHeight="50">
                                        <DataGrid.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <VirtualizingStackPanel VirtualizationMode="Recycling"/>
                                            </ItemsPanelTemplate>
                                        </DataGrid.ItemsPanel>
                                        <DataGrid.Columns>
                                            <DataGridTemplateColumn Width="100" 
                                                                    CellTemplate="{StaticResource ThumbnailColumnTemplate}"/>
                                            <DataGridTextColumn Binding="{Binding Name}" 
                                                                Width="*">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
                                                        <Setter Property="Padding" Value="10,0"/>
                                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                                        <Setter Property="FontFamily" Value="{StaticResource EternalUiFont}"/>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTemplateColumn Width="100">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{Binding VQ3Time}" 
                                                                Tag="vq3" 
                                                                Click="PlayDemo_Click"
                                                                Style="{StaticResource TimeButtonStyle}"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Width="100">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="{Binding CPMTime}" 
                                                                Tag="cpm" 
                                                                Click="PlayDemo_Click"
                                                                Style="{StaticResource TimeButtonStyle}"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Width="50">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="★" 
                                                                Click="ToggleFavorite_Click"
                                                                Foreground="{Binding IsFavorite, Converter={StaticResource FavoriteColorConverter}}"
                                                                Background="Transparent"
                                                                BorderThickness="0"
                                                                FontSize="16"
                                                                Padding="0"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Width="100">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="VQ3" 
                                                                Tag="vq3" 
                                                                Click="LaunchMap_Click"
                                                                Style="{StaticResource ActionButtonStyle}"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                            <DataGridTemplateColumn Width="100">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Content="CPM" 
                                                                Tag="cpm" 
                                                                Click="LaunchMap_Click"
                                                                Style="{StaticResource ActionButtonStyle}"/>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </ScrollViewer>
                            </Grid>
                        </TabItem>
                        
                        <TabItem x:Name="FavoritesTab" Header="Favorites">
                            <ScrollViewer x:Name="FavoritesScroll" 
                                          VerticalScrollBarVisibility="Auto" 
                                          ScrollChanged="FavoritesScroll_ScrollChanged"
                                          PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                <DataGrid x:Name="FavoritesGrid" 
                                          AutoGenerateColumns="False" 
                                          HeadersVisibility="None">
                                    <DataGrid.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel VirtualizationMode="Recycling"/>
                                        </ItemsPanelTemplate>
                                    </DataGrid.ItemsPanel>
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Width="100" 
                                                                CellTemplate="{StaticResource ThumbnailColumnTemplate}"/>
                                        <DataGridTextColumn Binding="{Binding Name}" 
                                                            Width="*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
                                                    <Setter Property="Padding" Value="10,0"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="FontFamily" Value="{StaticResource EternalUiFont}"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding VQ3Time}" 
                                                            Tag="vq3" 
                                                            Click="PlayDemo_Click"
                                                            Style="{StaticResource TimeButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding CPMTime}" 
                                                            Tag="cpm" 
                                                            Click="PlayDemo_Click"
                                                            Style="{StaticResource TimeButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="50">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="★" 
                                                            Click="ToggleFavorite_Click"
                                                            Foreground="{Binding IsFavorite, Converter={StaticResource FavoriteColorConverter}}"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            FontSize="16"
                                                            Padding="0"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="VQ3" 
                                                            Tag="vq3" 
                                                            Click="LaunchMap_Click"
                                                            Style="{StaticResource ActionButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="CPM" 
                                                            Tag="cpm" 
                                                            Click="LaunchMap_Click"
                                                            Style="{StaticResource ActionButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                        </TabItem>
                        
                        <TabItem x:Name="RecentTab" Header="Recent">
                            <ScrollViewer x:Name="RecentScroll"
                                          VerticalScrollBarVisibility="Auto"
                                          PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                                          ScrollChanged="RecentScroll_ScrollChanged">
                                <DataGrid x:Name="RecentGrid" 
                                          AutoGenerateColumns="False" 
                                          HeadersVisibility="None">
                                    <DataGrid.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel VirtualizationMode="Recycling"/>
                                        </ItemsPanelTemplate>
                                    </DataGrid.ItemsPanel>
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Width="100" 
                                                                CellTemplate="{StaticResource ThumbnailColumnTemplate}"/>
                                        <DataGridTextColumn Binding="{Binding Name}" 
                                                            Width="*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource TextColor}"/>
                                                    <Setter Property="Padding" Value="10,0"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="FontFamily" Value="{StaticResource EternalUiFont}"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding VQ3Time}" 
                                                            Tag="vq3" 
                                                            Click="PlayDemo_Click"
                                                            Style="{StaticResource TimeButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="{Binding CPMTime}" 
                                                            Tag="cpm" 
                                                            Click="PlayDemo_Click"
                                                            Style="{StaticResource TimeButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="50">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="★" 
                                                            Click="ToggleFavorite_Click"
                                                            Foreground="{Binding IsFavorite, Converter={StaticResource FavoriteColorConverter}}"
                                                            Background="Transparent"
                                                            BorderThickness="0"
                                                            FontSize="16"
                                                            Padding="0"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="VQ3" 
                                                            Tag="vq3" 
                                                            Click="LaunchMap_Click"
                                                            Style="{StaticResource ActionButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTemplateColumn Width="100">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="CPM" 
                                                            Tag="cpm" 
                                                            Click="LaunchMap_Click"
                                                            Style="{StaticResource ActionButtonStyle}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                        </TabItem>
                        
                        <TabItem Header="Settings">
                            <ScrollViewer VerticalScrollBarVisibility="Auto"
                                        PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                                <StackPanel Margin="10">
                                    <TextBlock Text="Player Name:" Margin="5,5,5,0" Foreground="{DynamicResource TextColor}"/>
                                    <TextBox x:Name="PlayerNameBox" 
                                            Margin="5"
                                            Background="{DynamicResource TextBoxBackground}"
                                            Foreground="{DynamicResource TextColor}"/>
                                    
                                    <CheckBox x:Name="ThemeToggle" 
                                            Content="Dark Theme" 
                                            Margin="5"
                                            Foreground="{DynamicResource TextColor}"
                                            IsChecked="True"
                                            Checked="ThemeToggle_Checked" 
                                            Unchecked="ThemeToggle_Unchecked"/>
                                    
                                    <Button Content="Save Settings" 
                                            Click="SavePlayerName_Click" 
                                            Margin="5"
                                            Style="{StaticResource ActionButtonStyle}"/>
                                    
                                    <Button Content="Rescan Maps" 
                                            Click="RescanMaps_Click" 
                                            Margin="5"
                                            Style="{StaticResource ActionButtonStyle}"/>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>
                    </TabControl>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Grid>




</Window>

